# Correction note for the handover document (`SEHE_Creative_and_Copy_Pack_2026-09-24.pdf`)

_The PDF and its editable source were **not** included in the material supplied for this packaging job, so the corrections below could not be applied directly. They are written so whoever holds the editable source can apply them. Where a correction needs evidence that was not supplied, that is stated rather than guessed._

## 1. Lead with what was completed and what the pack contains
Open with the inventory: three creative sets (Meta Brand/Season, Meta 14-Day Tour, Google PMax) totalling 119 export entries, plus 13 canvas-only creatives, all now rendered and validated; the editable React/JSX sources with every image asset; the PMax build sheet; the creative-system spec. Move the export-failure history to a short "availability note" (the original browser export produced transparent PNGs; the cause is proven and documented in the package README; the creatives have been re-exported with a replacement exporter).

## 2. Work status — distinguish planned, approved, launched, paused, unlaunched
Only the following can be stated from the supplied material; anything beyond it needs evidence from the ad accounts or the client:
- **Google PMax "Journeys" campaign**: the build sheet (`PMax_Copy_and_Deployment.md`) is explicitly a **plan for a first test** ("Use the supplied text … for the first test"). No evidence of launch was supplied. Present it as *planned / ready to launch*, not as running.
- **Meta Brand/Season and 14-Day sets**: rendered creatives exist; the supplied notes say the client was still to choose between MOF v1 and v2, and MOF v3 is labelled "layout-system tests". No launch, approval or pause evidence was supplied. Present as *produced, awaiting approval* unless the accounts show otherwise.
- **Website work** is live (separately verified this month) and is not part of this pack.
Reconcile the campaign-status summary table with each campaign's narrative so the same campaign is not "live" in one place and "planned" in another.

## 3. Budgets
Label every figure as either **planned** (a proposal that was never spent) or **historical** (actually spent, with the period). The only budget figure in the supplied material is a planned starting budget for the PMax campaign in the website repo's campaign sheet (NZ$70/day, a recommendation). Do not present it as spend.

## 4. Brand/Season conventions are not universal rules
The Brand/Season set's conventions — "Now Booking · 2026–2028" pill, homepage/Journeys destinations, brand-level (not tour-specific) copy — apply to that set. The 14-Day Tour set deliberately uses tour-specific years ("2026/27") and tour pricing, and the PMax set uses two asset groups with two different final URLs (Journeys page; homepage). Write each set's conventions under that set rather than as a global rule.

## 5. The "Brochure Collection Page" conversion
Describe it as **a URL-based custom conversion that counts visits to the brochure-collection page**. It does **not** independently verify that a brochure form was submitted; a visitor can reach that URL by other routes. Treat it as a proxy for brochure leads until form submissions are tracked directly.

## 6. File-availability list (per item, as actually found)
| Item | Status |
|---|---|
| Editable React/JSX sources (49 files, 36 image assets) | **Included** — verbatim, plus an offline-capable copy |
| Rendered PNGs, 119 exporter entries | **Included** — re-exported and validated |
| 13 canvas-only creatives (Walter Peak, MOF v1 ×6, MOF v2 ×6) | **Included** — labelled as extras / historical alternatives |
| Contact sheets, manifest with SHA-256, expected→source→output mapping | **Included** |
| Creative System Spec, Project Notes, PMax build sheet, image map, tour list, brochure URLs, two brochure PDFs | **Included** — as supplied |
| `SEHE_Creative_and_Copy_Pack_2026-09-24.pdf` and its editable source | **Missing** — not supplied; this note stands in for the corrections |
| Separate TOF copy PDF / spreadsheet | **Missing / awaiting retrieval** — referenced, not supplied |
| Pinnacle reporting documents | **Missing / awaiting retrieval** — referenced, not supplied |
| `previews/` older shareable snapshot and `originals-supplied/` raw uploads | **Available separately** — omitted from the two upload archives by the sender to reduce size |
| Original `SEHE_Creatives_2026-09-23.zip` | Superseded — all 119 PNGs were fully transparent; replaced by this package's exports |
| The Canva design | **Available separately** — in the client's Canva account; not part of this source |
| Font files | **Included** in the offline copy (self-hosted woff2); the original loaded them from Google Fonts at runtime |

## 7. Historical copy and facts
Historical ad copy is preserved exactly as written. Prices, dates, availability, reviews, awards, inclusions and booking terms appearing in the creatives are listed in `FACTS_TO_VERIFY.md` and **have not been freshly verified** as part of this packaging.
