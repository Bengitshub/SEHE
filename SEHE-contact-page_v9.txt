<!-- =====================================================================
     SIR EDMUND HILLARY EXPLORER — CONTACT PAGE (/contact)
     COMPLETE PAGE  ·  VERSION v9  ·  2026-09-25  (paste into ONE Duda HTML widget)
     -----------------------------------------------------------------------
     WHERE IT GOES (the whole page is ONE widget):
       page "contact-new" (duplicate of /contact). Delete the banner row, the
       "Contact Us" text row and the Facebook/Instagram row. In the form's row
       delete the "Send us an enquiry" text and the Trustpilot widget, and KEEP
       THE FORM exactly as it is (button stays "Send"). Add ONE full-width row
       (padding 0) DIRECTLY ABOVE the form's row, add ONE HTML widget, paste
       this whole file. Set the form row's padding to 0 too.
       On the published page the form moves into the right-hand column. In the
       Duda editor it stays in its own row below: that is expected.
       Steps + rollback: notes/2026-09-25-old-pages-rebuild-runbook.md
     -----------------------------------------------------------------------
     CHANGELOG (latest first; v1 to v6 are in the repo history):
       v9 — The real cause of the vanishing, found on the live contact-new
            (25 Sep 2026): the copied text stopped at line 150, inside the
            CSS, so the page's HTML never reached Duda (it happened to v7
            and to one v8 attempt; the complete v8 paste works). The code
            was never the problem. v9 adds an END OF FILE line as the last
            line: if it is the last line in Duda's code box, the whole file
            is pasted. Copy from the downloaded file, never from a preview.
            Page code identical to v8.
       v8 — v7 vanished in the Duda editor after Update. v8 uses only markup
            already working in this site's Duda widgets: the contact list is
            a plain list (no definition-list tags), the CSS has no child
            combinators (Duda publishes that character as an HTML entity,
            which silently drops the rule), and the docking script polls
            the way the live tour pages' script does. Copy unchanged.
       v7 — CONSOLIDATED: the three blocks (A/B/C) became ONE HTML widget;
            on the published site a small script moves the native form into
            the right-hand column (the tour pages' docking method).
     -----------------------------------------------------------------------
     PASTE FROM LATEST/: contact-page.txt (one file, one widget).
     RULES KEPT: native form untouched (recipient, subject, webhook, field
     IDs, labels, reCAPTCHA, success message); every phone and email link is
     a plain link (GTM "Phone Link Clicked" fires on phone-link clicks);
     nothing here can show .dmform-success (GTM "Contact Us Form Submitted"
     keys on it); no rating hard-coded (live TrustBox only); no extra
     bootstrap; no chat system added (tawk.to already loads site-wide); no
     schema; no tracking; no departure data; CSS scoped to .sehe-pg-contact.
     ===================================================================== -->
<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&amp;family=Open+Sans:wght@400;600;700&amp;display=swap" rel="stylesheet">
<style>
  /* ── Duda host row ─────────────────────────────────────────────────────
     Let the hero run full-bleed and stop the row adding a white strip under
     the transparent site header. :has() rules sit on their own so a browser
     without :has() drops only these lines (the runbook also sets the row's
     padding to 0 in the editor, which covers those browsers). */
  #dm .dmContent .dmRespRow:has(.sehe-pg-contact) { padding-top: 0 !important; padding-bottom: 0 !important; overflow: visible !important; max-width: none !important; }
  #dm .dmContent .dmRespRow:has(.sehe-pg-contact) .dmRespColsWrapper { overflow: visible !important; max-width: none !important; }
  #dm .dmContent .dmRespRow:has(.sehe-pg-contact) .dmRespCol { overflow: visible !important; max-width: none !important; padding-left: 0 !important; padding-right: 0 !important; }
  #dm .dmContent .dmCustomHtml:has(.sehe-pg-contact) { overflow: visible !important; }

  /* ── Page scope + tokens (Journeys v22 palette) ─────────────────────── */
  .sehe-pg-contact, .sehe-pg-contact *, .sehe-pg-contact *::before, .sehe-pg-contact *::after { box-sizing: border-box; }
  .sehe-pg-contact {
    --navy: #153b67; --navy-dark: #0e2d52; --gold: #c8a56c; --gold-light: #d4b37e;
    --gold-ink: #7a5f30; /* gold for small text on light backgrounds (5.9:1 on white) */
    --off-white: #f9f7f3; --cream: #faf8f3; --text: #1a1a1a; --text-2: #454545;
    --border: #d8d3c8; --border-light: #e8e4db; --white: #ffffff;
    display: block; width: 100%; margin: 0 !important; padding: 0 !important;
    font-family: 'Open Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
    font-size: 17px; line-height: 1.65; color: var(--text); text-align: left !important;
    -webkit-text-size-adjust: 100%; text-size-adjust: 100%; overflow-wrap: break-word;
  }
  /* theme rule ".dmFullRowRespTmpl section{padding:50px 40px}" would re-pad every section */
  .sehe-pg-contact section, .sehe-pg-contact aside, .sehe-pg-contact nav { padding: 0 !important; margin: 0; }
  .sehe-pg-contact h1, .sehe-pg-contact h2, .sehe-pg-contact h3, .sehe-pg-contact h4 {
    font-family: 'Montserrat', -apple-system, 'Segoe UI', Arial, sans-serif !important; font-weight: 600 !important;
    color: var(--navy) !important; text-transform: none !important; text-align: left !important;
    letter-spacing: -0.01em !important; margin: 0 !important; padding: 0 !important;
  }
  .sehe-pg-contact p, .sehe-pg-contact li, .sehe-pg-contact dt, .sehe-pg-contact dd, .sehe-pg-contact figcaption, .sehe-pg-contact blockquote, .sehe-pg-contact summary, .sehe-pg-contact td, .sehe-pg-contact th {
    font-family: 'Open Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif !important;
    text-align: left !important; text-transform: none !important;
  }
  .sehe-pg-contact p { font-size: 17px !important; line-height: 1.7 !important; color: var(--text-2) !important; margin: 0 !important; }
  .sehe-pg-contact a { color: var(--navy) !important; text-decoration: underline !important; text-underline-offset: 3px; text-decoration-thickness: 1px; }
  .sehe-pg-contact a:hover { color: var(--navy-dark) !important; text-decoration-thickness: 2px; }
  .sehe-pg-contact a:focus-visible, .sehe-pg-contact summary:focus-visible, .sehe-pg-contact button:focus-visible { outline: 3px solid var(--navy) !important; outline-offset: 3px !important; border-radius: 4px; }
  .sehe-pg-contact img { max-width: 100%; height: auto; border: 0; }
  .sehe-pg-contact .sp-sr { position: absolute !important; width: 1px !important; height: 1px !important; margin: -1px !important; padding: 0 !important; overflow: hidden !important; clip: rect(0 0 0 0) !important; white-space: nowrap !important; border: 0 !important; }

  /* ── Hero (same build as the Journeys / Brochure heroes) ────────────── */
  .sehe-pg-contact .sp-hero {
    position: relative; width: 100vw; margin-left: calc(50% - 50vw) !important; margin-right: calc(50% - 50vw) !important;
    padding: 0 !important; min-height: max(540px, 68vh); min-height: max(540px, 68svh);
    display: flex; align-items: flex-end; background-color: #081525; overflow: hidden;
  }
  .sehe-pg-contact .sp-hero--compact { min-height: max(380px, 46vh); min-height: max(380px, 46svh); }
  .sehe-pg-contact .sp-hero-img { position: absolute; top: 0; left: 0; width: 100% !important; height: 100% !important; max-width: none !important; object-fit: cover; object-position: center 50%; display: block; }
  .sehe-pg-contact .sp-hero-veil { position: absolute; top: 0; right: 0; bottom: 0; left: 0; background: linear-gradient(180deg, rgba(8,21,37,.62) 0%, rgba(8,21,37,.26) 28%, rgba(8,21,37,.58) 56%, rgba(8,21,37,.95) 100%); }
  .sehe-pg-contact .sp-hero--compact .sp-hero-veil { background: linear-gradient(180deg, rgba(8,21,37,.70) 0%, rgba(8,21,37,.52) 40%, rgba(8,21,37,.92) 100%); }
  /* the site header's logo slot is a transparent placeholder until you scroll —
     put the real roundel in its lane so the brand is there on load */
  .sehe-pg-contact .sp-hero-logo { position: absolute; top: 26px; left: 0; width: 16.66% !important; height: 92px !important; max-width: none !important; object-fit: contain; object-position: center; z-index: 2; filter: drop-shadow(0 2px 10px rgba(8,21,37,.5)); }
  .sehe-pg-contact .sp-hero-inner { position: relative; z-index: 1; width: 100%; max-width: 1240px; margin: 0 auto; padding: 150px 30px 58px; }
  .sehe-pg-contact .sp-hero--compact .sp-hero-inner { padding: 140px 30px 46px; }
  .sehe-pg-contact .sp-eyebrow {
    display: inline-flex; align-items: center; gap: 16px; font-family: 'Montserrat', sans-serif !important;
    font-size: 13.5px !important; font-weight: 700 !important; letter-spacing: .42em !important; text-transform: uppercase !important;
    color: #d4b37e !important; margin: 0 0 22px !important; text-shadow: 0 1px 12px rgba(8,21,37,.95);
  }
  .sehe-pg-contact .sp-eyebrow::before, .sehe-pg-contact .sp-eyebrow::after { content: ''; width: 44px; height: 1px; background: #d4b37e; flex: none; }
  .sehe-pg-contact .sp-hero h1 { font-weight: 700 !important; font-size: clamp(40px, 5.2vw, 66px) !important; line-height: 1.08 !important; color: #ffffff !important; text-shadow: 0 2px 26px rgba(8,21,37,.55); }
  .sehe-pg-contact .sp-hero--compact h1 { font-size: clamp(36px, 4.2vw, 54px) !important; }
  .sehe-pg-contact .sp-hero-sub { margin: 22px 0 0 !important; max-width: 620px; font-size: clamp(17px, 1.5vw, 19px) !important; line-height: 1.65 !important; color: rgba(255,255,255,.95) !important; text-shadow: 0 1px 14px rgba(8,21,37,.6); }
  .sehe-pg-contact .sp-hero a:focus-visible, .sehe-pg-contact .sp-band a:focus-visible { outline-color: var(--gold-light) !important; }

  /* ── Body, section heads ────────────────────────────────────────────── */
  .sehe-pg-contact .sp-body { max-width: 1240px; margin: 0 auto !important; padding: 64px 30px 76px !important; }
  .sehe-pg-contact .sp-section + .sp-section, .sehe-pg-contact .sp-section + .sp-band, .sehe-pg-contact .sp-band + .sp-section { margin-top: 76px !important; }
  .sehe-pg-contact .sp-head { max-width: 780px; margin: 0 0 28px !important; }
  .sehe-pg-contact .sp-kicker { display: block; font-family: 'Montserrat', sans-serif !important; font-size: 14px !important; font-weight: 700 !important; letter-spacing: .14em !important; text-transform: uppercase !important; color: var(--gold-ink) !important; margin: 0 0 10px !important; }
  .sehe-pg-contact .sp-head h2 { font-size: clamp(28px, 3vw, 36px) !important; line-height: 1.2 !important; margin: 0 0 12px !important; }
  .sehe-pg-contact .sp-head p { max-width: 60ch; }   /* ~66-72 characters per line in Open Sans */
  .sehe-pg-contact .sp-head p + p { margin-top: 10px !important; }

  /* ── Buttons (48px+ tap targets) ────────────────────────────────────── */
  .sehe-pg-contact .sp-actions { display: flex; flex-wrap: wrap; gap: 12px; }
  .sehe-pg-contact .sp-btn {
    display: inline-flex; align-items: center; justify-content: center; gap: 8px; min-height: 50px; padding: 12px 24px;
    border-radius: 6px; border: 2px solid var(--navy); background: var(--navy); color: #ffffff !important;
    font-family: 'Montserrat', sans-serif !important; font-size: 16px !important; font-weight: 600 !important; line-height: 1.25 !important;
    text-decoration: none !important; text-align: center !important; cursor: pointer; transition: background-color .2s, color .2s, border-color .2s;
  }
  .sehe-pg-contact .sp-btn:hover { background: var(--navy-dark); border-color: var(--navy-dark); color: #ffffff !important; }
  .sehe-pg-contact .sp-btn--ghost { background: transparent; color: var(--navy) !important; }
  .sehe-pg-contact .sp-btn--ghost:hover { background: var(--navy); color: #ffffff !important; }
  .sehe-pg-contact .sp-btn--gold { background: var(--gold); border-color: var(--gold); color: var(--navy-dark) !important; }
  .sehe-pg-contact .sp-btn--gold:hover { background: var(--gold-light); border-color: var(--gold-light); color: var(--navy-dark) !important; }
  .sehe-pg-contact .sp-btn--light { background: transparent; border-color: rgba(255,255,255,.85); color: #ffffff !important; }
  .sehe-pg-contact .sp-btn--light:hover { background: #ffffff; border-color: #ffffff; color: var(--navy-dark) !important; }

  /* ── Disclosure ("show more") — native details element, works with JS off.
     Plain descendant selectors only: Duda's publisher rewrites the child
     combinator as an HTML entity, and the browser then drops the whole rule. ── */
  .sehe-pg-contact details.sp-more summary {
    list-style: none; display: inline-flex; align-items: center; gap: 12px; min-height: 50px; padding: 12px 22px;
    border: 2px solid var(--navy); border-radius: 6px; background: #ffffff; color: var(--navy) !important;
    font-family: 'Montserrat', sans-serif !important; font-size: 16px !important; font-weight: 600 !important; cursor: pointer;
  }
  .sehe-pg-contact details.sp-more summary::-webkit-details-marker { display: none; }
  .sehe-pg-contact details.sp-more summary::after { content: '+'; font-size: 22px; line-height: 1; font-weight: 400; }
  .sehe-pg-contact details.sp-more[open] summary::after { content: '\2212'; }
  .sehe-pg-contact details.sp-more summary:hover { background: var(--navy); color: #ffffff !important; }
  .sehe-pg-contact .sp-when-open { display: none; }
  .sehe-pg-contact details[open] summary .sp-when-open { display: inline; }
  .sehe-pg-contact details[open] summary .sp-when-closed { display: none; }

  /* ── Navy help band (the Journeys contact card, scoped) ─────────────── */
  .sehe-pg-contact .sp-band { background: var(--navy); border: 1px solid var(--gold); border-radius: 12px; padding: 34px 36px !important; display: flex; flex-wrap: wrap; align-items: center; justify-content: space-between; gap: 22px 36px; }
  .sehe-pg-contact .sp-band-copy { flex: 1 1 420px; }
  .sehe-pg-contact .sp-band h2 { color: #ffffff !important; font-size: clamp(24px, 2.4vw, 28px) !important; line-height: 1.25 !important; margin: 0 0 8px !important; }
  .sehe-pg-contact .sp-band p { color: rgba(255,255,255,.88) !important; max-width: 62ch; }
  .sehe-pg-contact .sp-band .sp-actions { flex: 1 1 440px; justify-content: flex-end; }

  @media (max-width: 860px) {
    .sehe-pg-contact .sp-hero { min-height: max(460px, 60svh); }
    .sehe-pg-contact .sp-hero--compact { min-height: max(340px, 42svh); }
    /* phones crop to the brightest part of the frame — darker veil keeps contrast */
    .sehe-pg-contact .sp-hero-veil, .sehe-pg-contact .sp-hero--compact .sp-hero-veil { background: linear-gradient(180deg, rgba(8,21,37,.66) 0%, rgba(8,21,37,.48) 30%, rgba(8,21,37,.72) 58%, rgba(8,21,37,.96) 100%); }
    .sehe-pg-contact .sp-hero-inner, .sehe-pg-contact .sp-hero--compact .sp-hero-inner { padding: 118px 20px 40px; }
    .sehe-pg-contact .sp-hero-logo { left: 50%; width: 56px !important; transform: translateX(-50%); top: 18px; height: 56px !important; }
    .sehe-pg-contact .sp-eyebrow { gap: 0; font-size: 12.5px !important; letter-spacing: .2em !important; margin-bottom: 14px !important; }
    .sehe-pg-contact .sp-eyebrow::before, .sehe-pg-contact .sp-eyebrow::after { display: none; }
    .sehe-pg-contact .sp-hero h1, .sehe-pg-contact .sp-hero--compact h1 { font-size: 34px !important; line-height: 1.14 !important; }
    .sehe-pg-contact .sp-hero-sub { font-size: 17px !important; line-height: 1.6 !important; margin-top: 14px !important; }
    .sehe-pg-contact .sp-body { padding: 44px 18px 56px !important; }
    .sehe-pg-contact .sp-section + .sp-section, .sehe-pg-contact .sp-section + .sp-band, .sehe-pg-contact .sp-band + .sp-section { margin-top: 56px !important; }
    .sehe-pg-contact .sp-band { padding: 24px 20px !important; }
    .sehe-pg-contact .sp-band .sp-actions { justify-content: flex-start; }
    .sehe-pg-contact .sp-actions .sp-btn { flex: 1 1 100%; }
  }
  @media (max-width: 540px) {
    .sehe-pg-contact .sp-hero h1, .sehe-pg-contact .sp-hero--compact h1 { font-size: 30px !important; }
    .sehe-pg-contact .sp-hero-inner, .sehe-pg-contact .sp-hero--compact .sp-hero-inner { padding: 106px 18px 34px; }
  }
  @media (prefers-reduced-motion: reduce) {
    .sehe-pg-contact *, .sehe-pg-contact *::before, .sehe-pg-contact *::after { transition: none !important; scroll-behavior: auto !important; }
  }

  /* ── Contact page ───────────────────────────────────────────────────── */
  .sehe-pg-contact .sp-hero--compact { min-height: max(340px, 40vh); min-height: max(340px, 40svh); }
  .sehe-pg-contact .sp-hero--compact .sp-hero-inner { padding: 136px 30px 42px; }
  .sehe-pg-contact .sp-hero-sub { max-width: 640px; }

  /* details (left) + the docked native form (right). Until the form is docked
     (or if it never is) the details take the full width and the form stays in
     its own Duda row below this widget. */
  .sehe-pg-contact .ct-body { padding-top: 56px !important; padding-bottom: 56px !important; }
  .sehe-pg-contact .ct-grid { display: grid; grid-template-columns: minmax(0, 1fr); gap: 40px; align-items: start; }
  .sehe-pg-contact.ct-docked .ct-grid { grid-template-columns: minmax(0, 5fr) minmax(0, 6fr); gap: 64px; }
  .sehe-pg-contact .ct-formside[hidden] { display: none !important; }
  .sehe-pg-contact .ct-quick-sec { margin-top: 56px !important; }
  .sehe-pg-contact .ct-details { display: block; }
  .sehe-pg-contact .ct-head h3 { font-family: 'Montserrat', -apple-system, 'Segoe UI', Arial, sans-serif !important; font-weight: 600 !important; font-size: clamp(26px, 2.6vw, 32px) !important; line-height: 1.2 !important; color: #153b67 !important; text-transform: none !important; text-align: left !important; letter-spacing: -0.01em !important; margin: 0 0 20px !important; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmform-title { font-family: 'Montserrat', -apple-system, 'Segoe UI', Arial, sans-serif !important; font-weight: 600 !important; font-size: clamp(26px, 2.6vw, 32px) !important; line-height: 1.2 !important; color: #153b67 !important; text-transform: none !important; text-align: left !important; letter-spacing: -0.01em !important; margin: 0 0 20px !important; }
  .sehe-pg-contact .ct-list { list-style: none !important; margin: 0 !important; padding: 0 !important; }
  .sehe-pg-contact .ct-item { list-style: none !important; margin: 0 !important; padding: 14px 0 !important; border-bottom: 1px solid var(--border-light); }
  .sehe-pg-contact .ct-item::before, .sehe-pg-contact .ct-item::marker { content: none !important; }
  .sehe-pg-contact .ct-item:first-child { padding-top: 0 !important; }
  .sehe-pg-contact .ct-item:last-child { border-bottom: 0 !important; padding-bottom: 0 !important; }
  .sehe-pg-contact .ct-item .ct-label { font-family: 'Montserrat', sans-serif !important; font-size: 14px !important; font-weight: 700 !important; letter-spacing: .04em; line-height: 1.4 !important; color: var(--gold-ink) !important; margin: 0 0 2px !important; }
  .sehe-pg-contact .ct-item .ct-value { margin: 0 !important; font-size: 18px !important; line-height: 1.5 !important; color: var(--text) !important; }
  .sehe-pg-contact .ct-item .ct-value a { display: inline-block; padding: 6px 0; min-height: 44px; font-weight: 600; }
  .sehe-pg-contact .ct-item--main .ct-value a { font-family: 'Montserrat', sans-serif !important; font-size: 26px !important; font-weight: 700 !important; text-decoration: none !important; color: var(--navy) !important; }
  .sehe-pg-contact .ct-item--main .ct-value a:hover { text-decoration: underline !important; }
  .sehe-pg-contact .ct-item--email .ct-value a { word-break: break-all; }
  .sehe-pg-contact .ct-item .ct-note { font-size: 15.5px !important; color: var(--text-2) !important; margin-top: 2px !important; }
  .sehe-pg-contact .ct-chat { margin: 22px 0 0; }
  .sehe-pg-contact .ct-chat[hidden] { display: none !important; }
  .sehe-pg-contact button.sp-btn { -webkit-appearance: none; appearance: none; }
  .sehe-pg-contact .ct-trust { display: flex; align-items: center; gap: 16px; margin: 26px 0 0; padding: 18px 0 0; border-top: 1px solid var(--border-light); }
  .sehe-pg-contact .ct-trust img { width: 72px !important; height: auto !important; flex: none; }
  .sehe-pg-contact .ct-trust-copy { display: flex; flex-direction: column; gap: 8px; min-width: 0; flex: 1 1 auto; }
  .sehe-pg-contact .ct-trust-copy p { font-size: 15.5px !important; line-height: 1.5 !important; }
  .sehe-pg-contact .ct-trustbox { width: 100%; max-width: 420px; min-height: 24px; }

  /* ── The native Duda form, once docked into .ct-formslot — LOOK ONLY ───
     The form stays a real Duda form: recipient, subject, Zapier webhook,
     field IDs (dmform-0/1/3), its field labels (they are the keys the email
     and the Zapier webhook receive), reCAPTCHA and the success message are
     not touched. Visible field names are CSS text beside the untouched
     labels, matched by field TYPE (Duda renames the fields while it sends,
     so a name match would vanish after a failed send). Descendant selectors
     only (see the kit note); reCAPTCHA's own hidden textarea is excluded by
     its class so it stays hidden. */
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform { font-family: 'Open Sans', -apple-system, 'Segoe UI', Arial, sans-serif !important; max-width: none !important; padding: 0 !important; margin: 0 !important; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform, #dm .dmContent .sehe-pg-contact .ct-formslot .dmform * { box-sizing: border-box !important; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmform-title { display: block !important; visibility: visible !important; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmform-wrapper, #dm .dmContent .sehe-pg-contact .ct-formslot .dmform form { margin: 0 !important; padding: 0 !important; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmforminput { float: none !important; width: 100% !important; margin: 0 0 18px !important; padding: 0 !important; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmforminput::before { display: block; margin: 0 0 6px; font-family: 'Montserrat', sans-serif; font-weight: 600; font-size: 15.5px; line-height: 1.3; color: #1a1a1a; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmforminput:has(input[type="text"])::before { content: 'Your name'; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmforminput:has(input[type="email"])::before { content: 'Your email address'; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmforminput:has(textarea:not(.g-recaptcha-response))::before { content: 'Your message'; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmforminput input[type="text"], #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmforminput input[type="email"], #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmforminput textarea:not(.g-recaptcha-response) {
    width: 100% !important; display: block !important; margin: 0 !important; padding: 13px 16px !important;
    background: #ffffff !important; color: #1a1a1a !important; border: 1px solid #8a8378 !important; border-radius: 6px !important;
    font-family: 'Open Sans', -apple-system, 'Segoe UI', Arial, sans-serif !important; font-size: 17px !important; line-height: 1.45 !important;
    height: auto !important; min-height: 52px !important; box-shadow: none !important; -webkit-appearance: none !important; appearance: none !important;
  }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmforminput textarea:not(.g-recaptcha-response) { min-height: 170px !important; resize: vertical !important; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmforminput input::placeholder, #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmforminput textarea::placeholder { color: #6b655c !important; opacity: 1 !important; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmforminput input[type="text"]:focus, #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmforminput input[type="email"]:focus, #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmforminput textarea:not(.g-recaptcha-response):focus { outline: 3px solid #153b67 !important; outline-offset: 1px !important; border-color: #153b67 !important; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmformsubmit { float: none !important; display: block !important; width: 100% !important; max-width: none !important; height: auto !important; min-height: 0 !important; margin: 8px 0 0 !important; padding: 0 !important; border: 0 !important; border-radius: 0 !important; background: none !important; box-shadow: none !important; text-align: left !important; line-height: normal !important; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmformsubmit::before, #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmformsubmit::after { display: none !important; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmformsubmit input[type="submit"] {
    width: 100% !important; display: block !important; height: auto !important; min-height: 54px !important; padding: 16px 24px !important; margin: 0 !important;
    background: #153b67 !important; border: 2px solid #153b67 !important; border-radius: 6px !important; color: #ffffff !important; cursor: pointer !important;
    font-family: 'Montserrat', sans-serif !important; font-size: 16px !important; font-weight: 600 !important; letter-spacing: .02em !important; text-transform: none !important; line-height: 1.2 !important;
    -webkit-appearance: none !important; appearance: none !important;
  }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmformsubmit input[type="submit"]:hover { background: #0e2d52 !important; border-color: #0e2d52 !important; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmformsubmit input[type="submit"]:focus-visible { outline: 3px solid #153b67 !important; outline-offset: 3px !important; }
  /* success / error messages: shown by Duda's own script; GTM's 'Contact Us
     Form Submitted' needs .dmform-success fully on screen, so keep it compact */
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmform-success, #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmform-error { margin: 0 !important; padding: 18px 20px !important; border-radius: 8px !important; font-family: 'Open Sans', sans-serif !important; font-size: 17px !important; line-height: 1.6 !important; color: #1a1a1a !important; text-align: left !important; background: #faf8f3 !important; border: 1px solid #e8e4db !important; border-left: 4px solid #c8a56c !important; }
  #dm .dmContent .sehe-pg-contact .ct-formslot .dmform .dmform-error { border-left-color: #b84040 !important; }

  /* quick links */
  .sehe-pg-contact .ct-quick { list-style: none !important; margin: 0 !important; padding: 0 !important; display: grid; grid-template-columns: repeat(auto-fit, minmax(min(100%, 260px), 1fr)); gap: 16px; }
  .sehe-pg-contact .ct-quick li { margin: 0 !important; padding: 0 !important; list-style: none !important; }
  .sehe-pg-contact .ct-quick li::before, .sehe-pg-contact .ct-quick li::marker { content: none !important; }
  .sehe-pg-contact .ct-quick a { display: flex; flex-direction: column; gap: 4px; height: 100%; min-height: 96px; padding: 18px 20px; border: 1px solid var(--border); border-radius: 10px; background: #ffffff; text-decoration: none !important; transition: border-color .2s, box-shadow .2s; }
  .sehe-pg-contact .ct-quick a:hover { border-color: var(--navy); box-shadow: 0 2px 10px rgba(21,59,103,.08); }
  .sehe-pg-contact .ct-quick strong { font-family: 'Montserrat', sans-serif !important; font-size: 17px; font-weight: 600; color: var(--navy); }
  .sehe-pg-contact .ct-quick span { font-size: 15.5px; line-height: 1.5; color: var(--text-2); }

  @media (max-width: 860px) {
    .sehe-pg-contact.ct-docked .ct-grid { grid-template-columns: minmax(0, 1fr); gap: 44px; }
    .sehe-pg-contact .ct-body { padding-top: 36px !important; padding-bottom: 40px !important; }
    .sehe-pg-contact .sp-hero--compact .sp-hero-inner { padding: 112px 20px 34px; }
    .sehe-pg-contact .ct-item--main .ct-value a { font-size: 24px !important; }
    .sehe-pg-contact .ct-chat .sp-btn { width: 100%; }
    .sehe-pg-contact .ct-quick-sec { margin-top: 44px !important; }
  }
</style>
<div class="sehe-pg-contact">
<section class="sp-hero sp-hero--compact" aria-labelledby="sehe-ct-title">
  <img class="sp-hero-img" src="https://lirp.cdn-website.com/35e9f777/dms3rep/multi/opt/L161-Milford-Sound-Fiordland-Rob-Suisted-1920w.jpg" srcset="https://lirp.cdn-website.com/35e9f777/dms3rep/multi/opt/L161-Milford-Sound-Fiordland-Rob-Suisted-640w.jpg 640w, https://lirp.cdn-website.com/35e9f777/dms3rep/multi/opt/L161-Milford-Sound-Fiordland-Rob-Suisted-1280w.jpg 1280w, https://lirp.cdn-website.com/35e9f777/dms3rep/multi/opt/L161-Milford-Sound-Fiordland-Rob-Suisted-1920w.jpg 1920w" sizes="100vw" alt="" style="object-position:center 62%" fetchpriority="high">
  <div class="sp-hero-veil" aria-hidden="true"></div>
  <img class="sp-hero-logo" src="https://lirp.cdn-website.com/35e9f777/dms3rep/multi/opt/SEH+Explorer_logo+white-350w.png" alt="Sir Edmund Hillary Explorer" width="350" height="350">
  <div class="sp-hero-inner">
    <p class="sp-eyebrow">Contact us</p>
    <h1 id="sehe-ct-title">Let&#39;s plan your New Zealand journey.</h1>
    <p class="sp-hero-sub">Ask us about choosing a tour, travelling solo, room options or an existing booking. Our New Zealand team is here to help.</p>
  </div>
</section>
<div class="sp-body ct-body">
  <h2 class="sp-sr">Ways to get in touch</h2>
  <div class="ct-grid">
    <div class="ct-details">
      <div class="ct-head">
        <h3>Contact details</h3>
      </div>
      <ul class="ct-list" role="list">
        <li class="ct-item ct-item--main">
          <p class="ct-label">Phone</p>
          <p class="ct-value"><a href="tel:+6439741812">+64 3 974 1812</a></p>
        </li>
        <li class="ct-item">
          <p class="ct-label">From within New Zealand</p>
          <p class="ct-value"><a href="tel:039741812">(03) 974 1812</a></p>
        </li>
        <li class="ct-item">
          <p class="ct-label">From Australia</p>
          <p class="ct-value"><a href="tel:00116439741812">0011 64 3 974 1812</a></p>
        </li>
        <li class="ct-item">
          <p class="ct-label">From the USA or Canada</p>
          <p class="ct-value"><a href="tel:0116439741812">011 64 3 974 1812</a></p>
        </li>
        <li class="ct-item ct-item--email">
          <p class="ct-label">Email</p>
          <p class="ct-value"><a href="mailto:info@pounamutourismgroup.com">info@pounamutourismgroup.com</a></p>
        </li>
        <li class="ct-item">
          <p class="ct-label">Postal address</p>
          <p class="ct-value">PO Box 19735, Woolston, Christchurch 8241, New Zealand</p>
          <p class="ct-note">For mail only.</p>
        </li>
      </ul>
      <div class="ct-chat" data-sehe-chat hidden>
        <button type="button" class="sp-btn sp-btn--ghost">Chat with us</button>
      </div>
      <div class="ct-trust">
        <img src="https://lirp.cdn-website.com/35e9f777/dms3rep/multi/opt/new-zealands-leading-tour-operator-2025-winner-shield-256-816b3264-1920w.png" alt="World Travel Awards: New Zealand&#39;s Leading Tour Operator 2025" width="256" height="256" loading="lazy">
        <div class="ct-trust-copy">
          <p>World Travel Awards winner: New Zealand&#39;s Leading Tour Operator 2025.</p>
        <div id="sehe-ct-trustbox" class="trustpilot-widget ct-trustbox" data-locale="en-NZ" data-template-id="5419b6ffb0d04a076446a9af" data-businessunit-id="67af6f95db89fc000f855205" data-style-height="20px" data-style-width="100%">
          <a href="https://nz.trustpilot.com/review/siredmundhillaryexplorer.com" target="_blank" rel="noopener">Read our reviews on Trustpilot<span class="sp-sr"> (opens in a new tab)</span></a>
        </div>
        </div>
      </div>
    </div>
    <div class="ct-formside" hidden>
      <div class="ct-formslot" data-sehe-contact-slot></div>
    </div>
  </div>
  <section class="sp-section ct-quick-sec" aria-labelledby="sehe-ct-quick">
    <div class="sp-head">
      <h2 id="sehe-ct-quick">Helpful information</h2>
    </div>
    <ul class="ct-quick" role="list">
      <li><a href="/f-a-q"><strong>Frequently asked questions</strong><span>Booking, payments and what to expect on tour</span></a></li>
      <li><a href="/journeys"><strong>Journeys and brochures</strong><span>Compare our tours, then request a brochure from any tour page</span></a></li>
      <li><a href="/reviews"><strong>Guest reviews</strong><span>Read what our guests say about their journey</span></a></li>
    </ul>
  </section>
</div>
</div>
<script>
(function () {
  var box = document.getElementById('sehe-ct-trustbox');
  if (!box) return;
  var tries = 0;
  function ready() {
    if (box.querySelector('iframe')) return true;
    if (window.Trustpilot) {
      if (typeof window.Trustpilot.loadFromElement === 'function') { window.Trustpilot.loadFromElement(box, true); return true; }
    }
    return false;
  }
  var timer = setInterval(function () {
    tries = tries + 1;
    if (ready()) { clearInterval(timer); return; }
    if (tries === 40) clearInterval(timer);
  }, 500);
})();
</script>
<script>
(function () {
  /* Contact page enhancement — add only, never remove.
     "Start a chat" appears only once tawk.to (already on every page) is ready.
     The native form is not touched by any script.
     No less-than sign and no ampersand in this script (Duda's publisher escapes both). */
  var tries = 0;
  function chat() {
    var box = document.querySelector('.sehe-pg-contact [data-sehe-chat]');
    if (!box) return true;
    if (window.Tawk_API) {
      if (typeof window.Tawk_API.maximize === 'function') {
        box.hidden = false;
        var btn = box.querySelector('button');
        if (btn) {
          if (!btn.getAttribute('data-bound')) {
            btn.setAttribute('data-bound', '1');
            btn.addEventListener('click', function () { window.Tawk_API.maximize(); });
          }
        }
        return true;
      }
    }
    return false;
  }
  var timer = setInterval(function () {
    tries = tries + 1;
    if (chat()) { clearInterval(timer); return; }
    if (tries === 60) clearInterval(timer);
  }, 500);
})();
</script>
<script>
(function () {
  /* Contact form DOCKING, the same method the live tour pages use for their
     brochure forms. The real Duda form sits in its own row directly BELOW
     this widget, so its recipient, subject, webhook, reCAPTCHA and success
     message are all Duda's own. On the published site this script moves
     that form into the right-hand column as soon as the page contains it,
     before Duda draws the reCAPTCHA checkbox. If a checkbox is already
     drawn, or anything is missing, nothing moves and the form simply stays
     below. In the Duda editor nothing moves. The script never changes the
     form itself. No less-than sign and no ampersand in this script. */
  if (!/(^|\.)siredmundhillaryexplorer\.com$/.test(window.location.hostname)) return;
  var root = document.querySelector('.sehe-pg-contact');
  var slot = root ? root.querySelector('[data-sehe-contact-slot]') : null;
  if (!slot) return;
  var side = slot.parentElement;
  var done = false, tries = 0, timer = null;
  function findForm() {
    var all = document.querySelectorAll('div[data-element-type="dContactUsRespId"]');
    for (var i = 0; i !== all.length; i += 1) {
      if (all[i].querySelector('input[type="email"]')) return all[i];
    }
    return null;
  }
  function hideIfEmpty(row) {
    if (!row.querySelector('[data-element-type]')) row.style.setProperty('display', 'none', 'important');
  }
  function tick() {
    if (done) return;
    var w = findForm();
    if (!w) return;
    done = true;
    window.clearInterval(timer);
    if (w.querySelector('iframe')) return;   /* reCAPTCHA already drawn: leave the form where it is */
    var row = w.closest('.dmRespRow');
    slot.appendChild(w);
    side.hidden = false;
    root.classList.add('ct-docked');
    if (row) {
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', function () { hideIfEmpty(row); });
      } else {
        hideIfEmpty(row);
      }
    }
  }
  timer = window.setInterval(function () {
    tries += 1;
    tick();
    if (tries === 300) window.clearInterval(timer);
  }, 50);
  document.addEventListener('DOMContentLoaded', tick);
  tick();
})();
</script>
<!-- END OF FILE: contact-page.txt v9. If this is the last line in the Duda code box, the whole file is pasted. -->
